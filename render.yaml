services:
  - type: web
    name: puzzle-master-web-site
    env: static
    buildCommand: npm --prefix frontend run build
    staticPublishPath: ./frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
